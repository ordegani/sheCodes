// const deletedReducer = (element, action) => {
//     if (action.type === "MOVIE_DELETED"){

//     favorites = favorites.filter((favorite) => favorite.title !== element.title);

//     };
//     return favorites;
// }

// export default deletedReducer;